/**
 * Given string str, the task is to find the minimum number of characters to be inserted to convert it to a palindrome.
 */


function minInsertionPalindrome(str)
{
    
}

let str = "abcd";
minInsertionPalindrome(str)
